# Python dependencies for IPTV Provider automation scripts
# Install with: pip3 install -r requirements.txt

# Web automation and scraping
# Pin to stable versions for Alpine Linux compatibility
undetected-chromedriver==3.5.5
selenium==4.16.0

# HTTP requests
requests==2.31.0

# Fake user agents for anti-detection
fake-useragent==1.4.0

# Database connection
psycopg2-binary==2.9.9

# Encryption/Decryption (for Laravel Crypt compatibility)
cryptography==41.0.7
