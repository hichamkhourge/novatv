; Appended to the [www] pool — overrides only request_terminate_timeout.
; Loaded as zzz-streaming.conf so it processes AFTER the default www.conf.
; Never forcefully kill a running request — IPTV streams can last hours.
[www]
request_terminate_timeout = 0
